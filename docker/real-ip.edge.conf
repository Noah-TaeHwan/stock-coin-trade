# Caddy(compose.edge.yml) 뒤에서: Caddy가 설정한 X-Forwarded-For 한 값을 클라이언트 IP로 쓴다.
# nginx 포트는 이 구성에서 호스트에 열리지 않으므로 사설 대역에서 온 요청은 Caddy뿐이다.
set_real_ip_from 10.0.0.0/8;
set_real_ip_from 172.16.0.0/12;
set_real_ip_from 192.168.0.0/16;
real_ip_header X-Forwarded-For;
