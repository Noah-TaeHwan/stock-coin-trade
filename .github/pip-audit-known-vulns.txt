# Known vulnerabilities that CI passes to `pip-audit --ignore-vuln`.
# Any vulnerability NOT listed here fails the build.
#
# Empty since 2026-09-25: the 26 IDs tracked on 2026-09-24 (flask, flask-cors,
# requests, python-dotenv, pillow) were fixed by upgrading in Phase 1-4; see
# docs/evidence/containers-nginx-deps-2026-09-25.md.
#
# Add an entry only with a tracked fix, one per line:
# <ID>  # <package> <version> <CVE> fixed in <version>
